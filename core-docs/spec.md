# Product Specification

## Vision

<!-- SETUP:
If the user provided an existing spec/brief, extract the vision from it.
Otherwise ask: "In one sentence, what does success look like for this product?"
-->

[Populated by /init-project]

## Problem

<!-- SETUP:
Ask the user: "What specific problem does this solve? Who has it?"
If they already described this in the product brief, extract it and confirm.
-->

[Populated by /init-project]

## Solution

<!-- SETUP:
Ask the user: "How does the product solve the problem? What's the core interaction or experience?"
-->

[Populated by /init-project]

## Features

<!-- SETUP:
Ask the user: "What are the key features? Even a rough list is fine -- we can
refine later. For each, just give me a name and one sentence on what it does."
Format as a table with status (planned/in progress/shipped).
If they have an existing feature list, import it and ask what's missing.
-->

| Feature | Description | Status |
|---------|-------------|--------|
| [Populated by /init-project] | | planned |

## Tech Stack

<!-- SETUP:
Pull from what was already captured in CLAUDE.md. Don't re-ask.
If additional technical details surfaced during the spec conversation, add them here.
This section can be more detailed than CLAUDE.md's tech stack.
-->

[Populated by /init-project -- mirrors and extends CLAUDE.md tech stack]

## Cost Structure

<!-- SETUP:
Ask: "Are there per-use costs to be aware of? (API calls, hosting, third-party services)"
If none, delete this section entirely.
-->

| Component | Cost | Notes |
|-----------|------|-------|
| | | |

## Current Status

<!-- SETUP:
Ask: "Where is this project right now? (idea, MVP, alpha, beta, production)"
-->

- **Stage:** [Populated by /init-project]
- **Last updated:** [date of bootstrap]
